// @remover/renderer — a 1-1 drop-in for @remotion/renderer's programmatic API.
export { bundle, type RemoverBundle } from './bundle';
export { getCompositions, selectComposition } from './select-composition';
export { renderMedia, type RenderMediaOptions } from './render-media';
export { renderStill } from './render-still';
export type { VideoConfig } from './types';
